<%= simple_form_for(@experience) do |f| %>
    <%= f.input :name %>
    <%= f.input :source %>
    <%= f.input :completed_on %>
    <%= f.select :skill_id, @all_skills.map { |skill| [skill.name, skill.id] }%> <br />
    <%= f.submit "Submit" %>
<% end %>
